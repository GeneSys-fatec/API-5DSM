spring.application.name=tecsys

# Active profile (trocar para "prod" quando MySQL estiver pronto)
spring.profiles.active=mock

# JWT Configuration
jwt.secret=tecsys-rf-planning-secret-key-change-in-production-2024
jwt.expiration-ms=86400000

# Server
server.port=8080
